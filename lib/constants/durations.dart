const fastAnimationDuration = Duration(milliseconds: 100);
const animationDuration = Duration(milliseconds: 300);
const slowAnimationDuration = Duration(milliseconds: 500);
const shimmerAnimationDuration = Duration(milliseconds: 400);
const tooltipDuration = Duration(minutes: 1);
